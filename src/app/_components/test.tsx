"use server"
import React from 'react';
import {create, find} from "@/app/_service/test_service";

const Test = () => {
    // create()
    const users = find()
    return (
        <div>

        </div>
    );
};

export default Test;